# 나라장터 장비 입찰공고 대시보드

GitHub Pages에서 화면을 제공하고 GitHub Actions가 나라장터 API 데이터를 주기적으로 갱신합니다.

## 설정

저장소의 `Settings → Secrets and variables → Actions`에서 `NARA_API_KEY` Secret을 추가하세요. API 키와 인증서 파일은 저장소에 업로드하지 않습니다.
